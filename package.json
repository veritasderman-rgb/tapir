{
  "name": "nedovareny-tapir",
  "version": "1.0.0",
  "private": true,
  "description": "Edukační simulátor šíření infekčních onemocnění (SEIR/SEIRV)",
  "workspaces": [
    "simulation-core",
    "web-ui"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=web-ui",
    "build": "npm run build --workspace=web-ui",
    "test": "npm run test --workspace=simulation-core",
    "test:ui": "npm run test --workspace=web-ui",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  }
}
